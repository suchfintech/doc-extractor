# doc-extractor
Document analyzer service — vision-based extraction of structured frontmatter from compliance documents. Uses Agno + Pydantic structured output, multi-agent classifier+specialist pipeline.
